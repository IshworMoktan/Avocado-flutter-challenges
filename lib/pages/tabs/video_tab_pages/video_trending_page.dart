import 'package:flutter/material.dart';

class VideoTrendingPage extends StatelessWidget {
  const VideoTrendingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('No Content to show ...'),
      ),
    );
  }
}
